# data/ (M0)

From Week 11: bar schema + `validate_bars`, `resample_ohlcv`, `find_gaps`, Parquet layout
(`symbol=/date=` partitions) and a `BarRepository` interface with an in-memory and a Parquet implementation.
SQL schema in `sql/schema.sql`.
