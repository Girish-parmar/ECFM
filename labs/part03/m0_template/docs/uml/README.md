# UML (Mermaid, versioned with the code)

Add: `domain_classes.md` (class diagram), `order_flow.md` (sequence: signal → risk → OMS → broker → fill),
`order_states.md` (state diagram from your OrderStateMachine).
